interface:
  display_name: 'Fetcher LLM Streaming'
  short_description: 'SSE and LLM token streams'
  default_prompt: 'Use $fetcher-llm-streaming to consume an SSE or LLM token stream with a [DONE] terminator.'
