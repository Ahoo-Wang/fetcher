---
type: llm
weight: 1
---

Grade the final answer against $fetcher-decorator-service. Pass only if every point holds:

- Declares the class with `@api('/users', { fetcher: 'api' })`.
- `getUser` uses `@get('/{id}')` with a `@path('id')` parameter; `list` uses `@get('')` (or equivalent) with `@query('page')`; `create` uses `@post('')` with `@body()`.
- Method bodies `throw autoGeneratedError(...)` as placeholders.
- Mentions that `experimentalDecorators` must be enabled in tsconfig and that `reflect-metadata` needs no manual import.
- Invents no API: every `@ahoo-wang/*` import, class, function, option and constant it uses is one the skill documents (in `SKILL.md` or `references/api.md`). Fail the response if it relies on a symbol, option or package that does not exist.
