# 分析视图宿主准入复核

本轮在现有候选版本上继续执行验收，不将本机测试替代实际部署验证。

## 本轮已执行

- `linyi-k8s/dev` 实际补偿服务：重新读取 OpenAPI，确认 snapshot/event Schema GET 和 aggregation POST 路由；既有 opt-in 测试 4 项通过，实际执行 8 类模板/表达式/元素范围查询。快照概览为 584,009 条，事件流概览为 1,364,784 批次。这是本轮请求时的聚合数量，不是静态数据集或性能保证。
- 超时、取消、诊断、权限撤销和恢复、未知创建回执：5 个测试文件、66 项通过。诊断观察者失败不影响业务，诊断不包含查询正文和业务错误正文。
- 独立 HTTP fixture 与真实浏览器 IndexedDB 演练通过：共享内容与个人顺序、丢失创建回执幂等恢复、权限撤销与恢复、超时/取消，以及 50 次跨标签页 CAS、回滚和持久重置。该 fixture 使用模拟身份，不代表真实补偿服务已完成身份隔离验收。
- 已检查现有 CI 配置：Node 20/22/24 单位测试矩阵、三浏览器交付验证和独立 Storybook 交互检查均已配置。本轮未提交或触发远端 CI，因此不声称当前工作区已有远端绿色构建。

日志：`/tmp/fetcher-admission-dev.log`、`/tmp/fetcher-admission-boundaries.log`、`/tmp/fetcher-admission-http-host.log`。

额外实测：通过 Service 转发，对 snapshot/event 的 Schema 和 aggregation 共 4 个端点携带刻意无效的 Bearer，均返回 HTTP 200。OpenAPI 未声明 security/securitySchemes，dev 中未发现直接指向该服务的 Ingress。这证明当前直连路径没有拒绝本轮错误凭据，不能据此断言外部网关同样无鉴权，也不能把它作为生产权限验收通过。

约 110 秒内分 12 轮、轮间间隔 10 秒，串行执行 24 次匿名 COUNT 查询：全部 HTTP 200，返回计数均为合法安全整数，单请求最大耗时 266.66ms（含本机转发和网络）。这是短时低频稳定性观察，不是压力测试或长期运行结论。原始时间戳、每次耗时和认证探针结果见 [本轮 dev 原始证据](assets/2026-09-11-analysis-dev-admission.json)。

## 实际部署需要完成的闭环

| 验收项         | 现有实现和下一步操作                                                                                                                       | 通过证据                                                                                  |
| -------------- | ------------------------------------------------------------------------------------------------------------------------------------------ | ----------------------------------------------------------------------------------------- |
| 认证和数据隔离 | 通过实际宿主鉴权入口调用，不使用绕过网关的 Service 转发作结论；使用两个已授权的隔离测试身份                                                | 无身份、无效/过期身份被拒绝；两个身份的查询和视图读取不能越过各自业务范围                 |
| 登录切换和撤权 | `useViewEngine.scopeKey` 使用宿主验证后的用户与访问范围；身份或访问范围改变时更新 key/卸载；本机演示的固定 `local-demo` 不能用于多用户接入 | A 请求挂起后切 B，A 迟到响应不能显示；退出和撤权后不再显示旧结果；B 不能读取 A 的私人配置 |
| 运行监控       | 将现有 `onDiagnostic` 接入宿主已有观测设施，使用 operationId、phase、elapsedMs、errorCode，不新增通用监控框架                              | 注入一次查询失败可在既有监控中定位；日志不携带凭据、过滤值和结果正文                      |
| 业务负载       | 在约定的宿主环境按真实并发、查询复杂度和持续时长运行；既有万行前端实验与本轮串行 API 检查均不替代服务负载验收                              | 保留实际负载、时长、错误率、延迟分位数和资源指标，并与业务 SLO 对照                       |
| 发布与恢复     | 对确定的宿主构建产物执行既有发布流程；未发布的首次上线应保留关闭分析入口或回退宿主版本的路径                                               | CI 对应同一提交；测试环境实际回退后入口和旧功能可用；配置保留或清理行为经过验证           |

当前补偿接入是显式连接的本机示例，个人配置存于 IndexedDB；本轮没有发现需要为了填补上述宿主信息而重构通用引擎的依据。实际宿主入口、两组隔离测试身份和监控/发布目标尚未提供，不能在前端库中虚构这些集成。
