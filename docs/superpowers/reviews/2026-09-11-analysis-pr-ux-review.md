# 分析视图 PR 前产品与 UX 审查

## 本轮确认并修复

- **P2：刷新打断数据表阅读。** 原分页状态绑定 plan/rows 引用，自动刷新会跳回首页。现在依据查询语义重置；同查询刷新保留页码，页数缩小则收敛到有效页。实例 key 保证跨实例隔离。测试覆盖深复制 query 与新 rows、数值变化、结果缩小和实际查询变化；独立复核通过。
- **编辑术语不够清晰。** 将泛称“字段、类型、显示名称”改为“分组字段、分组方式、维度名称”；统计指标对应“统计字段、计算方式、指标名称”。可访问名称保持稳定。
- **无选项的统计对象占位。** 无元素范围可选且未选中失效范围时，不显示固定“根记录”配置行。失效范围仍保留修复入口；执行口径继续保留统计对象。

## 交互检查

1. 已保存分析首先展示结果和查询摘要；编辑配置按需展开。
2. 商品 ID 与商品名称在同一维度中配置，名称不混入统计指标；同名不同 ID 保持独立。
3. 保存与运行分别表达持久化配置和执行查询。未运行修改时保留原结果并提示。
4. 图表/数据表底部切换；自动刷新、全屏入口与数据视图一致，Esc 可退出全屏。
5. 数据表刷新保持阅读位置；查询变化回到首页。

## 查询与生命周期独立复核

审查了 AnalysisCommands、AnalysisView、analysisCompiler、analysisProjection 和共享 ViewRefreshControls。未确认自动刷新提交草稿、ANY 权限绕过或标签指标超限绕过；上面的分页问题已修复并复核。

## 本地证据

- 本轮定向 UX/分页测试 40 项通过。
- 真实 dev 接口 6 项通过，包含 Schema、分页、筛选、聚合、表达式、事件元素范围。只读请求；未执行补偿命令。
- 全仓构建、Storybook 静态构建、19 个导航目标与 171 个回归入口校验通过。
- 最终 `pnpm test:unit` 通过；view-engine 源码和 React Compiler 各 1234 passed、2 skipped。
- 完整 Storybook 354 passed，90 文件通过、2 个既有文件跳过。
- ESLint、双语 wiki 构建、11 项文档测试通过。

此为桌面界面、代码和自动化审查，不代表已经完成业务用户可用性研究或生产环境准入。
