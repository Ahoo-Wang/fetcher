# Visualization capability and progressive configuration

Approved scope: replace the analysis workbench type Select with five icon/name cards, then data mappings and collapsed styles. Preserve query/save/result/mode semantics and paused monitoring.

- Reuse projection validation as the single authority. Enumerate supported axis and single-measure witnesses, including later metrics and swapped axes. Infer recommendation from ungrouped/time/category shape without choosing for the user.
- Keep capability facts separate from the current presentation. Ambiguous candidates remain selectable; saved invalid intent stays visible until explicit repair.
- Keep React responsibilities to radio-card interaction and mapping/style controls. No wizard, new chart, global state or query implementation.
- Verify negative-first/positive-later measures, series cardinality, invalid temporal rows, duplicate groups, AVG semantics, persisted invalid mappings, keyboard selection, no-query transitions, and existing runtime/race regressions.
