# Architecture Review Repairs

用户已授权修复九组经过复现的问题。本计划在现有工作树实施，保留未提交的内置筛选器和单元格；不提交、不推送，不新增外部依赖。

- [x] 创建恢复：保留原 requestId 和提交快照到结果验证完成；未知结果通过同一 create 重放确认，不按列表内容认领实例。拒绝重试不能清除先前不确定请求；完整 load 后原请求仍可恢复。保存与恢复通过现有 InstanceWork 协调。
- [x] LocalStorageViewHost：删除当前访问范围中已不存在的实例幂等成功；现存实例继续校验权限/版本。保留显式 defaultInstanceId=null，create 不写默认偏好。
- [x] React 交付：根 ESM 和 /core 共享同一核心模块身份；保留 UMD。加入公开产物跨入口 Context 验证。
- [x] useExecutePromise：先摘下旧 controller，再发送同步 abort 通知，保护重入产生的新请求。
- [x] 日期范围编译：复用既有严格校验，仅归一明确空值，畸形端点不得变为 MATCH_ALL。
- [x] 远程选择：新选择的标签快照来自当前候选，保留原有未改变选择，禁止持久化展示装饰。
- [x] 多值文本：粘贴先按选区/光标替换，再拆分提交。
- [x] 日期单元格：统一可接受的无偏移/带偏移文本解析；不把不支持形式回退到机器时区。
- [x] 综合验证：对应红绿回归、包测试与编译模式、类型和 lint、React/view-engine 构建与公开包验证、Storybook/浏览器恢复、API 文档同步。

React 入口与 Hook、筛选器三处缺陷分别并行处理；主任务负责 Host 恢复、日期显示、文档和最终验证。先执行限定测试，完整套件与构建统一协调，避免同时清理 dist。

验证记录（2026-09-08）：

- 全仓库 `pnpm test:unit` 通过；最终两处恢复修正后重新运行 view-engine 完整测试，普通/React Compiler 模式各 479 项及类型检查通过。React 包 523 项测试通过。
- `pnpm lint:view-engine`、React/view-engine 构建与公开产物验证通过；view-engine 归档 SHA-256 为 `c49fa15b828879bbb5040604d60acbcc2669d7b296151d77067c126e5758b7c8`。
- Storybook 浏览器测试 246 项通过，保留 1 个已有跳过文件；静态构建及静态浏览器检查通过，包括远程选项标签、单元格配置刷新恢复。
- LocalStorage 与 HTTP 宿主端到端脚本通过：组件属性恢复、五类扩展、业务数据隔离、丢失创建回执重试、权限撤回保留草稿、跨标签页 CAS。首次与构建并行运行时两项脚本曾等待勾选框超时；构建完成后分别重跑原脚本通过，未据此修改业务逻辑。
- 复核并修复同步取消通知期间已有副本草稿被覆盖、仅提供列表接口的宿主无法按明确创建 ID 恢复两处回归，并加入测试。
- API 说明已同步；未提交、未推送。
