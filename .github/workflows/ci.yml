name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: ${{ github.workflow }}-${{ github.event.pull_request.number || github.ref }}
  cancel-in-progress: ${{ github.event_name == 'pull_request' }}

jobs:
  changes:
    uses: ./.github/workflows/changes.yml
  build-and-test:
    needs: changes
    if: needs.changes.outputs.code == 'true'
    timeout-minutes: 45
    runs-on: ubuntu-latest

    strategy:
      fail-fast: false
      matrix:
        node-version: [20, 22, 24]

    steps:
      - name: Checkout code
        uses: actions/checkout@v7

      - name: Setup pnpm
        uses: pnpm/action-setup@0977fd99725f1db4007ccb2928dbb4e90d06cc86 # v6
        with:
          version: 10.34.5

      - name: Setup Node.js
        uses: actions/setup-node@v7
        with:
          node-version: ${{ matrix.node-version }}
          cache: 'pnpm'

      - name: Install dependencies
        run: pnpm install --frozen-lockfile

      - name: Build
        run: pnpm -r --filter './packages/*' build

      - name: Test with coverage
        if: matrix.node-version == 24
        run: VITEST_MAX_WORKERS=1 pnpm test:unit

      - name: Test compatibility without coverage
        if: matrix.node-version != 24
        run: VITEST_MAX_WORKERS=1 pnpm -r --filter './packages/*' run test:no-coverage

      - name: Upload coverage reports to Codecov
        if: matrix.node-version == 24
        uses: codecov/codecov-action@fb8b3582c8e4def4969c97caa2f19720cb33a72f # v7
        with:
          token: ${{ secrets.CODECOV_TOKEN }}
          files: packages/*/coverage/coverage-final.json
          fail_ci_if_error: true

      - name: Preserve coverage for diagnosis
        if: always() && matrix.node-version == 24
        uses: actions/upload-artifact@v6
        with:
          name: unit-coverage-node24
          path: packages/*/coverage/coverage-final.json
          retention-days: 7
          if-no-files-found: ignore
